const baseUrl = `https://testapi.betterwood.com`
const hotelDetailUrl = `https://testapi.betterwood.com/#/hotelDetail`

const VIP_URL = `${baseUrl}`
const HOTEL_URL = `${baseUrl}/hotel`
const APP_URL = `${baseUrl}/base/app`
const CMS_URL = `${baseUrl}/cms/app`
const BDW_USER_URL = `${baseUrl}/user`
const CONTENT_URL = `${baseUrl}/content`

export {
  baseUrl,
  VIP_URL,
  HOTEL_URL,
  BDW_USER_URL,
  APP_URL,
  CMS_URL,
  CONTENT_URL,
  hotelDetailUrl,
}
